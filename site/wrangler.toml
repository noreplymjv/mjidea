name = "mjidea"
compatibility_date = "2026-08-25"
# Used when Cloudflare Root directory = site (Pages-style).
# Repo-root Workers deploys use ../wrangler.toml → ./site/dist
pages_build_output_dir = "dist"

